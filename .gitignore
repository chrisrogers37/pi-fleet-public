# Secrets — never commit
*.env
.env
.env.*
.mcp.json

# OAuth credentials
.google_workspace_mcp/

# Claude Code state (auth, channels, sessions) — but allow template dirs
.claude/
!manager/.claude/
!manager/.claude/**
!examples/worker/.claude/
!examples/worker/.claude/**

# Node modules
node_modules/

# Cloned project repos
*/projects/

# Logs
*.log

# OS
.DS_Store
